# PhysGaussian中期答辩演讲稿最终版

6min的PPT + 3min答辩

第一页：

各位评委老师好，我们团队的项目叫做“溯时凝态，执物通感：基于3D Gaussian的动态场景物理重建与交互”，我是项目负责人周梓昱。

第二页：

近些年来，3DGS一直是三维重建的热门方法，它渲染速度快、质量高，被广泛的用在测绘、数字人以及游戏制作等领域。而PhysGaussian则是进一步的把3DGS与物理仿真结合，使得重建出来的物体具有碰撞、形变等动态物理效果，未来可以应用到更多更广泛的物理仿真场景中。

而我们的项目，正是在这一背景下，去探索基于3D Gaussian的动态场景物理重建与交互。

第三页：标题   相关工作——论文的调研复现

从立项以来，我们团队研读了十余篇、复现了3DGS、4DGS、PhysGaussian、Vid2Sim、PhysSplat这五篇核心相关论文。其中我按照研究顺序重点讲一下3DGS、PhysGaussian、PhysSplat这三篇。3DGS 是一种显式的三维辐射场重建技术，它将场景表示为数百万个各向异性的高斯椭球，在保证了照片级极高渲染质量的同时实现了极快的实时渲染速度。PhysGaussian继承了3DGS的思想，它使用物质点法，进一步把每个高斯看作可参与 MPM 物理仿真的粒子，提出了这两个公式：$x_p(t)=\phi(X_p,t)$和$a_p(t)=F_p(t)A_pF_p(t)^T$。第一个公式是高斯点中心位置随时间的变化，第二个公式是用形变梯度$F_p$去更新每个高斯点的协方差。PhysSplat 则是在 PhysGaussian 的基础上，利用多模态大模型实现了物理参数的零样本预测。其核心流程利用 BLIP、MLLM 与 CLIP 构建感知链路，并进一步引入了 MPDP 与 PGAS 模块以优化属性分布与计算效率。

第四页：标题：  研究进展——Pipeline

这张图是我们项目的Pipeline。首先，输入是多角度的静态照片；然后经过八个Stage，其中Stage1\~3是第一阶段，在COLMAP预处理之后，完成图片的分割、修补与重建；Stage4\~5是第二阶段，是调用多种模型完成物理参数的预测和粒子填充；Stage6\~8是第三阶段，实现多个物体的拼接、MPM求解与物理交互。最终的输出是任意相机位姿的交互视频。

第五页：标题：  研究进展——核心创新点一

我们的第一个核心创新点是：提出了“先分割，后重建”的处理范式。当前学术界大量工作往往遵循“先重建整个场景，再进行 3D 语义分割”的思路，这极易导致物体边界处产生严重的高斯球特征粘连，阻碍了后续独立的物理场仿真。为了解决这一痛点，我们颠覆了传统流程：首先，引入 SAM3 视觉大模型在 2D 图像域实现高精度的目标提取；随后，结合多视角约束，将 2D 掩码反向投影至原始的三维稀疏点云中，在空间层面上进行精确切割，剥离出独立物体的纯净骨架。

这个技术思路带来了两大显著优势：一个是彻底消除了物体间的几何边界粘连；另一个是它原生性地契合了后续物理模拟的需求。我们在最前面的数据阶段就完成了目标彻底解耦，使系统后续能为不同物体赋予完全不同的物理参数，从而为复杂的多对象动态场景物理交互奠定了数据基础。

第六页：标题：  研究进展——核心创新点二

我们的第二个核心创新点是：引入了基于多模态大模型的物理参数预测。鉴于本项目实现了 3D 分割机制——能够为不同物体划分边界并赋予物理参数，因此，我们借鉴了 PhysSplat 但采取了更轻量化的策略：摒弃了计算开销较大的分布预测（MPDP）与自适应采样（PGAS）机制 ，聚焦于其核心的 MLLM 感知流程 。

在具体实现上，我们构建了一条严谨的参数估计与交叉验证流程：首先由 BLIP 提取场景图像特征并生成对应的文本描述 ；随后，MLLM 结合图像与文本描述，推理出一系列候选材质预测 ；接着，引入 CLIP 计算图像与各材质间的相似度，从而筛选出置信度最高的最优材质 ；最终，由 MLLM 依托该精准材质，输出对应的杨氏模量等核心物理仿真参数 。

第七页：标题：  研究进展——核心创新点三 

（我考虑不单独一页放视频，放在这一页，同时贴上归一化的实现代码；后面还要做不同材质、先后顺序）

第三个创新点是：在第一阶段分割分开重建的基础上，实现多个物体的交互。

由于不同物体是分别分割重建得到的，其坐标系、大小、中心位置还有朝向往往是不一致的。我们创新性的引入了归一化思想，不直接将不同.ply文件送入MPM求解，而是先进行如中心平移、朝向调整等预处理之后再将不同对象统一放到MPM求解器中处理，这样才能做到数值稳定、边界可控。

交互部分的参数设定也是一个难点，因为真正的物理交互需要做到视觉上合理、物理上可解。目前我们是借鉴PhysSplat的思想，通过模型预测 + 人工调整完成参数的设定。

此外，我们还在底层物理求解的基础上设置了“交互编排层”，它能将用户能理解的高级交互指令转化成底层物理参数 ，为后续实现多模态指令奠定了基础。

这个视频是我们整个Pipeline实现的DEMO，值得一提的是，这两辆小车是我们自己构建的数据集，我们可以通过给小车设置不同的材料、不同的速度产生不同的碰撞效果。（可能需要剪视频）

第八页：现有挑战及未来计划

我们的现有挑战和未来计划根据核心创新点的设置分为三个部分。

分割、修补与重建部分主要有两点。

第一，视场遮挡和多视角补全不一致的问题。在把物体单独分割出来时，物体的背面、底面等区域往往拍不到，导致重建出来的三维物体某些局部严重模糊。我们一开始尝试用扩散模型对 2D 图片进行补全，但发现不同视角下生成的内容不一定一致，放到 3DGS 里联合重建时，就容易出现模糊、阴影错乱和几何不稳定的问题。后续我们计划对图片生成模型进行微调，确保在补全多张视角图片时尽量保持内容一致，从源头上提高多视角修复的稳定性；同时加入图像置信度评判机制，根据置信度设置不同的更新系数进行迭代优化。

第二，静态光影物体表面绑定的问题。目前 3DGS 重建时会把原始拍摄环境里的光照和阴影一起“硬编码”到物体表面上。这样在静态展示时问题不明显，但如果物体在物理交互中发生翻滚、碰撞或大幅移动，原来固定在表面的阴影就可能不符合新的位置和光源方向，造成视觉穿帮。后续我们计划在流程中增加材质-光照解耦模块，引入本征图像分解技术，将环境光照与表面反射率深度解耦，使物体在动态交互时能够呈现符合物理规律的动态光影效果。

第九页：现有挑战及未来计划

物理参数前馈预测部分主要是参数甜区问题。尽管引入了多模态大模型实现了物理参数的自动化估计，但面对复杂的场景，预测的参数不能保证准确落入交互效果最自然的甜区范围，手动调参不可避免。我们计划一是缩小预测目标空间，将物理参数、交互运动参数分层处理，而不是让模型输出所有参数，二是引入更系统的参数搜索和候选比较机制，以获得更好的物理真实度。

交互部分主要有两个。第一是背景的真实化问题，目前的交互背景都是白色，因为目前将交互对象放在原先分割重建的自然场里面的再动态移动的时候噪点和残影较多，效果不是很好。我们计划结合前面的inpainting思路，对原物体位置出现的空缺进行修补，同时区分出静态的背景高斯和动态的物体高斯；第二是初始对齐问题，目前多个物体的初始对齐有时候仍需要人工干预，我们计划在保留归一化的情况下，加入第一帧的自动检验和位姿的自动调整。

第十页：我们团队的陈述到此为止，请各位老师批评指正！

